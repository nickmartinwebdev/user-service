# Test Environment Configuration
# This file contains environment variables for running tests

# Test Database Configuration
DATABASE_URL=postgresql://user_service_test:test_password@localhost:5433/user_service_test

# Test Database Connection Settings
DB_MAX_CONNECTIONS=5
DB_MIN_CONNECTIONS=1
DB_CONNECT_TIMEOUT=10
DB_IDLE_TIMEOUT=300
DB_MAX_LIFETIME=1800

# Application Settings for Tests
PORT=3001
HOST=127.0.0.1

# Logging Configuration for Tests
RUST_LOG=user_service=debug,sqlx=warn
LOG_LEVEL=debug

# Test-specific Settings
RUST_BACKTRACE=1
RUST_TEST_THREADS=1

# Security Settings for Tests (use faster bcrypt for tests)
BCRYPT_COST=4

# Test Database Schema
TEST_DATABASE_SCHEMA=public
